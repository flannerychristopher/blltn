document.getElementById('membership').innerHTML = "<%= j (
    render 'memberships/destroy.html'
) %>";

document.getElementById('users').innerHTML = "<%= j (
    render 'users/users.html', users: @users
) %>";